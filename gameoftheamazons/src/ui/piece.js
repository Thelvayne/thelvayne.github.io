import React from 'react'

const Piece = (props) => {
    /**
     * imageUrl
     * color
     * id
     * color of player
     * whether or not its your turn
     * if this players turn, color of this play is the same color as this piece,
     *   then we can make this piece draggable
     */
     return (
    <img draggable/>
  )
}

export default Piece
 
